{
  "name": "@instant-postgres/neon",
  "version": "0.1.0",
  "private": true,
  "main": "./index.ts",
  "types": "./index.ts",
  "license": "MIT",
  "scripts": {
    "clean": "rm -rf .turbo node_modules",
    "lint": "biome lint --apply .",
    "format": "biome format --write .",
    "typecheck": "tsc --noEmit",
    "typegen": "pnpm dlx openapi-typescript https://neon.tech/api_spec/release/v2.json -o ./src/schema.d.ts"
  },
  "dependencies": {
    "openapi-fetch": "^0.9.5"
  },
  "devDependencies": {
    "@biomejs/biome": "^1.5.3",
    "@instant-postgres/tsconfig": "workspace:^0.1.0",
    "typescript": "^5.2.2"
  }
}